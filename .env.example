# Copy to .env for local development.

# Where Robert keeps his moods and projects. With no database reachable the
# API falls back to the sample data in data/seed.js (reads only).
MONGODB_URI=mongodb://127.0.0.1:27017/robert

# Port the Express server listens on. The Vite dev server proxies /api here.
PORT=5000
